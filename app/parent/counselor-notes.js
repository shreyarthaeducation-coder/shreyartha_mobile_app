import { CounselorNotesScreen } from '../../components/parent';

/** Counselor Notes — read-only list of shared counselling sessions. */
export default function ParentCounselorNotes() {
  return <CounselorNotesScreen />;
}
