import { ParentPendingScreen } from '../../components/parent';

/** Shown until an admin verifies the parent account and links it to a child. */
export default function ParentPending() {
  return <ParentPendingScreen />;
}
