import { ParentMenuScreen } from '../../components/parent';

/** Parent home — the tile grid. Replaces the old full-page WebView at /dashboard/parent. */
export default function ParentHome() {
  return <ParentMenuScreen />;
}
