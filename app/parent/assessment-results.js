import { AssessmentResultsScreen } from '../../components/parent';

/** Assessment Results — personal statement + psychometric completion. */
export default function ParentAssessmentResults() {
  return <AssessmentResultsScreen />;
}
