import { AcademicProgressScreen } from '../../components/parent';

/**
 * Academic Progress — Part A (selected chapters and topics) plus the analytics overview shared
 * with the student panel's My Analytics.
 */
export default function ParentAcademicProgress() {
  return <AcademicProgressScreen />;
}
