import ParentNotificationsScreen from '../../components/parent/ParentNotificationsScreen';

/** The parent's notifications — school events for the child's class. Also where a tapped push lands. */
export default function ParentNotifications() {
  return <ParentNotificationsScreen />;
}
