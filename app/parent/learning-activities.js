import { LearningActivitiesScreen } from '../../components/parent';

/**
 * Learning Activities — resources, homework and personalised material as three flat lists, over
 * the composite endpoint the website ignores.
 */
export default function ParentLearningActivities() {
  return <LearningActivitiesScreen />;
}
