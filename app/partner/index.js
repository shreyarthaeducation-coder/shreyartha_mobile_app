import { PartnerMenuScreen } from '../../components/partner';

/** Partner home — the tile grid. Replaces the old full-page WebView at /dashboard/partner. */
export default function PartnerHome() {
  return <PartnerMenuScreen />;
}
