import { UpskillScreen } from '../../components/staff';

/** Native Upskill Your Self — the Teacher Skills Edge content tree. */
export default function TeacherUpskill() {
  return <UpskillScreen homeRoute="/teacher" />;
}
