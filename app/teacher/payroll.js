import { PayrollScreen } from '../../components/staff';

/** Native Payroll Management — published payslips plus the server-generated PDF. */
export default function TeacherPayroll() {
  return <PayrollScreen homeRoute="/teacher" />;
}
