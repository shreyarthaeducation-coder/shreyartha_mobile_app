import { MyCalendarScreen } from '../../components/staff';

/** Native My Calendar — school events, national holidays and your own leave on one month grid. */
export default function TeacherMyCalendar() {
  return <MyCalendarScreen homeRoute="/teacher" />;
}
