import { LeaveScreen } from '../../components/staff';

/** Native Leave Management — balances, requests and the apply sheet over /api/staff/hr/leave. */
export default function TeacherLeave() {
  return <LeaveScreen homeRoute="/teacher" />;
}
