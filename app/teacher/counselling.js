import { CounsellingNotesScreen } from '../../components/staff';

/** Native Counselling Needs and Notes — academic and parent sessions. */
export default function TeacherCounselling() {
  return <CounsellingNotesScreen homeRoute="/teacher" />;
}
