import { EventsScreen } from '../../components/student';

/** Native Events & Info — the month calendar with events, holidays and the student's attendance. */
export default function StudentEvents() {
  return <EventsScreen />;
}
