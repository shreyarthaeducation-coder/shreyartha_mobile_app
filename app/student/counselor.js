import { CounselorScreen } from '../../components/student';

/**
 * Speak to Counselor. Reached from the student home header, not a tile — same as the web, where it
 * lives in the header rather than the sidebar.
 */
export default function StudentCounselor() {
  return <CounselorScreen />;
}
